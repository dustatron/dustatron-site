---
// Component imports and setup can go here
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>My Astro Site with Decap CMS</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        padding: 2rem;
        max-width: 800px;
        margin: 0 auto;
        line-height: 1.6;
      }
      h1 {
        color: #333;
      }
      .admin-link {
        display: inline-block;
        margin-top: 2rem;
        padding: 0.5rem 1rem;
        background-color: #4a5568;
        color: white;
        text-decoration: none;
        border-radius: 0.25rem;
      }
      .admin-link:hover {
        background-color: #2d3748;
      }
    </style>
    <script is:inline>
      // This script will run in the browser
      document.addEventListener("DOMContentLoaded", function () {
        // Get the admin link element
        var adminLink = document.querySelector(".admin-link");
        // Check if we're on GitHub Pages (not localhost)
        if (
          window.location.hostname !== "localhost" &&
          window.location.hostname !== "127.0.0.1"
        ) {
          // Update the href to include the base path
          adminLink.setAttribute("href", "/dustatron-site/admin");
        }
      });
    </script>
  </head>
  <body>
    <h1>Welcome to My Astro Site with Decap CMS</h1>
    <p>
      This is a simple Astro site integrated with Decap CMS for content
      management.
    </p>
    <a href="/admin" class="admin-link">Go to Admin Dashboard</a>
  </body>
</html>
